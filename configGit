1ere etape :
    - faire un fork si le projet ne vous appartient pas
    sinon creer le repertoire sur github

2eme etape :
    - verifier si git est installe
    - copier l'url de votre depot (bouton vert)
    - tapez la commande : git clone URL (profil utilisateur)

3eme etape :
    - pour configurer git, entrez les commandes suivantes :
        git config --global user.name "NOM"
        git config --global user.email "EMAIL"
            
git config --local user.name "CelticJ"
git config --local user.email "EMAIL"